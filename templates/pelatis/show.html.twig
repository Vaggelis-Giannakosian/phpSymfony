{% extends 'base.html.twig' %}

{% block title %}Pelatis{% endblock %}

{% block body %}
    <h1>Pelatis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pelati.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pelati.name }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ pelati.lastName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pelatis_index') }}">back to list</a>

    <a href="{{ path('pelatis_edit', {'id': pelati.id}) }}">edit</a>

    {{ include('pelatis/_delete_form.html.twig') }}
{% endblock %}
